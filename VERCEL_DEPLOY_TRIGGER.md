# Vercel deployment trigger

Portfolio deployment sync marker. The production site should deploy from `main`.

- RentBee removed from the project archive.
- Sync Meta Flow and LalaPay included.
- Profile image uses the GitHub avatar endpoint.
- Favicon is `favicon.svg`.
